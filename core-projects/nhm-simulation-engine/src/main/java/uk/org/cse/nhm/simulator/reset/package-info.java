/**
 * This package contains a handful of interlinked special actions and functions
 * which allow the imputation rules to be expressed in scenario language.
 * They belong together because they use letting in a non-standard way to pass
 * Particular walls/doors/windows around.
 */
package uk.org.cse.nhm.simulator.reset;
