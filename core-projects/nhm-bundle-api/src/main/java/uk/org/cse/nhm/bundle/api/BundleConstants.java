package uk.org.cse.nhm.bundle.api;

/**
 * Constants used when registering an NHM service with an OSGi
 * platform as keys in the bundle's metadata.
 */
public class BundleConstants {
    public static final String VERSION = "nhm.version";
}
