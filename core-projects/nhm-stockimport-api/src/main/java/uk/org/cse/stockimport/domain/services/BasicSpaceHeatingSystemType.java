package uk.org.cse.stockimport.domain.services;

/**
 * An enum which breaks the {@link SpaceHeatingSystemType} into the basic types
 * of device it describes.
 *
 * @author hinton
 * @since 1.0
 */
public enum BasicSpaceHeatingSystemType {
    BACK_BOILER,
    BOILER,
    HEAT_PUMP,
    COMMUNITY,
    STORAGE_HEATER,
    ROOM_HEATER,
    WARM_AIR_SYSTEM
}
